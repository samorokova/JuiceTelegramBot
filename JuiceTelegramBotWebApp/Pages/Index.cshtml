@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<form method="post">
    <table class="table">
        <thead>
            <tr>
                <th></th>
                <th>Name</th>
                <th>Is custom</th>
                <th>Approved</th>
                <th>Added by</th>
                <th>Creation date</th>
                <td></td>
            </tr>
        </thead>
        <tbody>
            @*@foreach (var juice in Model.Juices)
            {
                <tr>
                    <td>@Html.EditorFor(m=>m.Juices)
                    </td>
                    <td>@juice.Name</td>
                    <td>@juice.IsCustom</td>
                    <td>@juice.Approved</td>
                    <td>@juice.UserName</td>
                    <td>@juice.JuiceDateTime</td>
                    <td>
                        <button type="submit" asp-page-handler="delete" asp-route-id="@juice.Name">delete</button>
                    </td>
                </tr>
            }*@
            @for (int i = 0; i < Model.Juices.Count; i++)
            {
            <tr>
                <td>@Html.EditorFor(m => m.Juices[i].IsChecked)</td>
                <td>@Model.Juices[i].Name</td>
                <td>@Model.Juices[i].IsCustom</td>
                <td>@Model.Juices[i].Approved</td>
                <td>@Model.Juices[i].UserName</td>
                <td>@Model.Juices[i].JuiceDateTime</td>
                <td><button type="submit" asp-page-handler="delete" asp-route-name="@Model.Juices[i].Name">delete</button></td>
            </tr>
            }
            <tr>
                <td>
                    <button type="submit" asp-page-handler="approved">Approve selected</button>
                </td>
                <td>
                    <button type="submit">Delete selected</button>
                </td>
            </tr>
        </tbody>
    </table>
    <a asp-page="./Createbeverage">Create a new beverage</a>
</form>